<local:DraggableWidget x:Class="StudyDashboard.TasksWidget"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:local="clr-namespace:StudyDashboard">
    
    <Border Style="{StaticResource WidgetStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Text="Today's Tasks" Style="{StaticResource HeaderTextStyle}"/>
            
            <!-- タスクリスト -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,5">
                <StackPanel x:Name="TasksPanel">
                    <CheckBox Content="Complete project documentation" Foreground="White" Margin="0,2"/>
                    <CheckBox Content="Review code changes" Foreground="White" Margin="0,2"/>
                    <CheckBox Content="Prepare presentation slides" Foreground="White" Margin="0,2" IsChecked="True"/>
                    <CheckBox Content="Team meeting at 3 PM" Foreground="White" Margin="0,2"/>
                </StackPanel>
            </ScrollViewer>
            
            <!-- 統計 -->
            <Border x:Name="StatsBorder" Grid.Row="2" Background="#40333333" Padding="8" Margin="0,5,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="CompletedTasksText" Text="1/4" FontWeight="Bold" Foreground="White"/>
                        <TextBlock x:Name="CompletedLabel" Text="Completed" Style="{StaticResource ContentTextStyle}" FontSize="10"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock x:Name="RemainingTasksText" Text="3" FontWeight="Bold" Foreground="White"/>
                        <TextBlock x:Name="RemainingLabel" Text="Remaining" Style="{StaticResource ContentTextStyle}" FontSize="10"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Add Taskボタン -->
            <Button x:Name="AddTaskButton" Grid.Row="3" Content="+ Add Task" 
                    Background="#FF4CAF50" Foreground="White" BorderThickness="0"
                    Padding="10,8" Margin="0,8,0,0" FontWeight="Bold"
                    Click="AddTaskButton_Click"/>
        </Grid>
    </Border>
    
    <!-- タスク追加ダイアログ（オーバーレイ） -->
    <local:DraggableWidget.Resources>
        <Style x:Key="DialogTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#40FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#60FFFFFF"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Margin" Value="0,3"/>
        </Style>
        <Style x:Key="DialogComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#40FFFFFF"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Margin" Value="0,3"/>
        </Style>
    </local:DraggableWidget.Resources>
</local:DraggableWidget>
